apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-gateway
  namespace: munshi
  annotations:
    linkerd.io/inject: enabled
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-gateway
      version: v1
  template:
    metadata:
      labels:
        app: postgres-gateway
        version: v1
      annotations:
        linkerd.io/inject: enabled
    spec:
      containers:
      - name: postgres
        image: postgres:15
        env:
        - name: POSTGRES_DB
          value: "gateway_db"
        - name: POSTGRES_USER
          value: "gateway_user"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-gateway-secret
              key: password
        ports:
        - containerPort: 5432
          name: postgres
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        livenessProbe:
          exec:
            command:
              - pg_isready
              - -U
              - gateway_user
              - -d
              - gateway_db
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
              - pg_isready
              - -U
              - gateway_user
              - -d
              - gateway_db
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-data
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-gateway
  namespace: munshi
  annotations:
    linkerd.io/inject: enabled
spec:
  selector:
    app: postgres-gateway
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  type: ClusterIP
---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-gateway-secret
  namespace: munshi
type: Opaque
data:
  password: Z2F0ZXdheV9wYXNzd29yZA== # base64 encoded "gateway_password"