apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-gateway
  namespace: munshi
  annotations:
    linkerd.io/inject: enabled
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-gateway
      version: v1
  template:
    metadata:
      labels:
        app: redis-gateway
        version: v1
      annotations:
        linkerd.io/inject: enabled
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
          name: redis
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "400m"
        volumeMounts:
        - name: redis-data
          mountPath: /data
      volumes:
      - name: redis-data
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: redis-gateway
  namespace: munshi
  annotations:
    linkerd.io/inject: enabled
spec:
  selector:
    app: redis-gateway
  ports:
  - name: redis
    port: 6379
    targetPort: 6379
  type: ClusterIP